import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ApprovalAwaitPage extends StatelessWidget {
  const ApprovalAwaitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Your Accout is being verified"),
      ),
    );
  }
}
